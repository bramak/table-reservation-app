{
  "name": "table-reservation-app",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/bramak/table-reservation-app.git",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.1",
    "node-sass": "^4.13.0"
  },
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 3001 --silent"
  },
  "devDependencies": {
    "concurrently": "^5.0.2",
    "json-server": "^0.15.1"
  }
}
